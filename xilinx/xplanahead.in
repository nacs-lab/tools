#!/bin/bash

LIBEXEC_DIR="@CMAKE_INSTALL_FULL_LIBEXECDIR@/xilinx"

die() {
    printf "$@" >&2
    exit 1
}

[[ -z "$1" ]] && die "Command not specified."

exec "${LIBEXEC_DIR}/planahead-${1}" "${@:2}"
